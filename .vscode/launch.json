{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.py (scans_512)",
            "type": "python",
            "request": "launch",

            "program": "${workspaceFolder}/initial_pipeline/main.py",

            "cwd": "${workspaceFolder}/initial_pipeline",

            "console": "integratedTerminal",

            "args": [
                "--input_folder_path",
                "../tiles/scans_512/",
                "--output_name",
                "scans_512"
            ],

            "justMyCode": false
        }
    ]
}
